import React from 'react';

const NavigationBar = (props) => {
	return (
		<div className="navigation-bar" styl={{ marginTop: '15px' }}>
			List of recommended learning materials for front-end traineeship
		</div>
	);
};

export default NavigationBar;
